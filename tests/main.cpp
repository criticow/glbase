#include <glb/glb.hpp>

int main()
{
  LOGGER_DEBUG(Random::choose(1, 50));
  LOGGER_DEBUG(Random::choose(1, 50));
  LOGGER_DEBUG(Random::choose(1, 50));
  LOGGER_DEBUG(Random::choose(1, 50));
  LOGGER_DEBUG(Random::choose(1, 50));

  return 0;
}